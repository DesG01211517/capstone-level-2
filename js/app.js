function getDetails(event) {
  event.preventDefault();
  console.log(event.target["companyInfo"].value);
}
//$("#flightDateButton, #hotelDateButton").datepicker({
//  format: "mm/dd/yyyy",
//autoclose: true,
//});
